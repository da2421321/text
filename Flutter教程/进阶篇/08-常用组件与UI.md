# 08 - 常用组件与 UI

> 覆盖日常开发中最常用的 Flutter 组件，对比 UniApp 组件。

---

## 1. 组件对比速查

| UniApp 组件 | Flutter Widget |
|------------|---------------|
| `<view>` | `Container` / `Column` / `Row` |
| `<text>` | `Text` |
| `<image>` | `Image` |
| `<button>` | `ElevatedButton` / `TextButton` |
| `<input>` | `TextField` |
| `<scroll-view>` | `SingleChildScrollView` / `ListView` |
| `<swiper>` | `PageView` / `CarouselSlider` |
| `<navigator>` | `GestureDetector` + `context.go()` |
| `<picker>` | `showModalBottomSheet` + 自定义 |
| `<switch>` | `Switch` |
| `<checkbox>` | `Checkbox` |
| `<radio>` | `Radio` |
| `<slider>` | `Slider` |
| `<progress>` | `LinearProgressIndicator` |
| `<loading>` | `CircularProgressIndicator` |
| `<toast>` | `SnackBar` |
| `<modal>` | `showDialog` |
| `<action-sheet>` | `showModalBottomSheet` |

---

## 2. 表单组件

### Switch（开关）

```dart
class SettingItem extends StatefulWidget {
  @override
  State<SettingItem> createState() => _SettingItemState();
}

class _SettingItemState extends State<SettingItem> {
  bool _notifications = true;
  bool _darkMode = false;

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        SwitchListTile(
          title: Text('消息通知'),
          subtitle: Text('接收推送通知'),
          value: _notifications,
          onChanged: (value) => setState(() => _notifications = value),
        ),
        SwitchListTile(
          title: Text('深色模式'),
          value: _darkMode,
          onChanged: (value) => setState(() => _darkMode = value),
        ),
      ],
    );
  }
}
```

### Checkbox（复选框）

```dart
class MultiSelect extends StatefulWidget {
  @override
  State<MultiSelect> createState() => _MultiSelectState();
}

class _MultiSelectState extends State<MultiSelect> {
  final List<Map<String, dynamic>> _options = [
    {'label': 'Flutter', 'checked': false},
    {'label': 'React Native', 'checked': false},
    {'label': 'UniApp', 'checked': true},
  ];

  @override
  Widget build(BuildContext context) {
    return Column(
      children: _options.map((option) => CheckboxListTile(
        title: Text(option['label']),
        value: option['checked'],
        onChanged: (value) {
          setState(() => option['checked'] = value);
        },
      )).toList(),
    );
  }
}
```

### Radio（单选）

```dart
class RadioGroup extends StatefulWidget {
  @override
  State<RadioGroup> createState() => _RadioGroupState();
}

class _RadioGroupState extends State<RadioGroup> {
  String _selected = 'male';

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        RadioListTile<String>(
          title: Text('男'),
          value: 'male',
          groupValue: _selected,
          onChanged: (value) => setState(() => _selected = value!),
        ),
        RadioListTile<String>(
          title: Text('女'),
          value: 'female',
          groupValue: _selected,
          onChanged: (value) => setState(() => _selected = value!),
        ),
      ],
    );
  }
}
```

### Slider（滑块）

```dart
class VolumeControl extends StatefulWidget {
  @override
  State<VolumeControl> createState() => _VolumeControlState();
}

class _VolumeControlState extends State<VolumeControl> {
  double _volume = 50;

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Text('音量: ${_volume.round()}'),
        Slider(
          value: _volume,
          min: 0,
          max: 100,
          divisions: 100,
          label: '${_volume.round()}',
          onChanged: (value) => setState(() => _volume = value),
        ),
      ],
    );
  }
}
```

### 表单验证（Form）

```dart
class RegisterForm extends StatefulWidget {
  @override
  State<RegisterForm> createState() => _RegisterFormState();
}

class _RegisterFormState extends State<RegisterForm> {
  final _formKey = GlobalKey<FormState>();
  final _emailController = TextEditingController();
  final _passwordController = TextEditingController();

  @override
  void dispose() {
    _emailController.dispose();
    _passwordController.dispose();
    super.dispose();
  }

  void _submit() {
    if (_formKey.currentState!.validate()) {
      // 表单验证通过
      print('邮箱: ${_emailController.text}');
    }
  }

  @override
  Widget build(BuildContext context) {
    return Form(
      key: _formKey,
      child: Column(
        children: [
          TextFormField(
            controller: _emailController,
            decoration: InputDecoration(
              labelText: '邮箱',
              prefixIcon: Icon(Icons.email),
            ),
            keyboardType: TextInputType.emailAddress,
            validator: (value) {
              if (value == null || value.isEmpty) return '请输入邮箱';
              if (!value.contains('@')) return '邮箱格式不正确';
              return null;  // 验证通过
            },
          ),
          SizedBox(height: 16),
          TextFormField(
            controller: _passwordController,
            decoration: InputDecoration(
              labelText: '密码',
              prefixIcon: Icon(Icons.lock),
            ),
            obscureText: true,
            validator: (value) {
              if (value == null || value.length < 6) return '密码至少6位';
              return null;
            },
          ),
          SizedBox(height: 24),
          SizedBox(
            width: double.infinity,
            child: ElevatedButton(
              onPressed: _submit,
              child: Text('注册'),
            ),
          ),
        ],
      ),
    );
  }
}
```

---

## 3. 轮播图（PageView）

```dart
// 类比 UniApp 的 <swiper>
class BannerSlider extends StatefulWidget {
  final List<String> images;
  const BannerSlider({required this.images});

  @override
  State<BannerSlider> createState() => _BannerSliderState();
}

class _BannerSliderState extends State<BannerSlider> {
  final _controller = PageController();
  int _currentPage = 0;
  Timer? _timer;

  @override
  void initState() {
    super.initState();
    // 自动轮播
    _timer = Timer.periodic(Duration(seconds: 3), (_) {
      final next = (_currentPage + 1) % widget.images.length;
      _controller.animateToPage(
        next,
        duration: Duration(milliseconds: 300),
        curve: Curves.easeInOut,
      );
    });
  }

  @override
  void dispose() {
    _timer?.cancel();
    _controller.dispose();
    super.dispose();
  }

  @override
  Widget build(BuildContext context) {
    return Stack(
      children: [
        // 轮播内容
        SizedBox(
          height: 200,
          child: PageView.builder(
            controller: _controller,
            itemCount: widget.images.length,
            onPageChanged: (index) => setState(() => _currentPage = index),
            itemBuilder: (context, index) => Image.network(
              widget.images[index],
              fit: BoxFit.cover,
            ),
          ),
        ),

        // 指示点
        Positioned(
          bottom: 8,
          left: 0,
          right: 0,
          child: Row(
            mainAxisAlignment: MainAxisAlignment.center,
            children: widget.images.asMap().entries.map((entry) {
              return Container(
                width: _currentPage == entry.key ? 16 : 8,
                height: 8,
                margin: EdgeInsets.symmetric(horizontal: 2),
                decoration: BoxDecoration(
                  borderRadius: BorderRadius.circular(4),
                  color: _currentPage == entry.key
                      ? Colors.white
                      : Colors.white.withOpacity(0.5),
                ),
              );
            }).toList(),
          ),
        ),
      ],
    );
  }
}
```

---

## 4. 下拉刷新 + 上拉加载

```dart
// 类比 UniApp 的 onPullDownRefresh + onReachBottom
class NewsListPage extends StatefulWidget {
  @override
  State<NewsListPage> createState() => _NewsListPageState();
}

class _NewsListPageState extends State<NewsListPage> {
  List<String> _items = List.generate(10, (i) => '新闻 ${i + 1}');
  bool _isLoadingMore = false;
  final _scrollController = ScrollController();

  @override
  void initState() {
    super.initState();
    _scrollController.addListener(_onScroll);
  }

  @override
  void dispose() {
    _scrollController.dispose();
    super.dispose();
  }

  void _onScroll() {
    if (_scrollController.position.pixels >=
        _scrollController.position.maxScrollExtent - 100) {
      _loadMore();
    }
  }

  Future<void> _refresh() async {
    await Future.delayed(Duration(seconds: 1));
    setState(() {
      _items = List.generate(10, (i) => '新闻 ${i + 1}（已刷新）');
    });
  }

  Future<void> _loadMore() async {
    if (_isLoadingMore) return;
    setState(() => _isLoadingMore = true);
    await Future.delayed(Duration(seconds: 1));
    setState(() {
      _items.addAll(List.generate(5, (i) => '更多新闻 ${_items.length + i + 1}'));
      _isLoadingMore = false;
    });
  }

  @override
  Widget build(BuildContext context) {
    return RefreshIndicator(
      onRefresh: _refresh,
      child: ListView.builder(
        controller: _scrollController,
        itemCount: _items.length + 1,
        itemBuilder: (context, index) {
          if (index == _items.length) {
            return Padding(
              padding: EdgeInsets.all(16),
              child: Center(
                child: _isLoadingMore
                    ? CircularProgressIndicator()
                    : Text('上拉加载更多'),
              ),
            );
          }
          return ListTile(title: Text(_items[index]));
        },
      ),
    );
  }
}
```

---

## 5. 图片选择与预览

```dart
// 需要安装: image_picker: ^1.0.0
import 'package:image_picker/image_picker.dart';

class ImagePickerDemo extends StatefulWidget {
  @override
  State<ImagePickerDemo> createState() => _ImagePickerDemoState();
}

class _ImagePickerDemoState extends State<ImagePickerDemo> {
  XFile? _image;
  final _picker = ImagePicker();

  Future<void> _pickImage(ImageSource source) async {
    final image = await _picker.pickImage(
      source: source,
      maxWidth: 1080,
      imageQuality: 80,
    );
    if (image != null) {
      setState(() => _image = image);
    }
  }

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        if (_image != null)
          Image.file(File(_image!.path), height: 200, fit: BoxFit.cover),
        Row(
          children: [
            ElevatedButton.icon(
              icon: Icon(Icons.camera_alt),
              label: Text('拍照'),
              onPressed: () => _pickImage(ImageSource.camera),
            ),
            SizedBox(width: 16),
            ElevatedButton.icon(
              icon: Icon(Icons.photo_library),
              label: Text('相册'),
              onPressed: () => _pickImage(ImageSource.gallery),
            ),
          ],
        ),
      ],
    );
  }
}
```

---

## 6. 动画

### 基础动画

```dart
// AnimatedContainer（类比 CSS transition）
class AnimatedBox extends StatefulWidget {
  @override
  State<AnimatedBox> createState() => _AnimatedBoxState();
}

class _AnimatedBoxState extends State<AnimatedBox> {
  bool _expanded = false;

  @override
  Widget build(BuildContext context) {
    return GestureDetector(
      onTap: () => setState(() => _expanded = !_expanded),
      child: AnimatedContainer(
        duration: Duration(milliseconds: 300),
        curve: Curves.easeInOut,
        width: _expanded ? 200 : 100,
        height: _expanded ? 200 : 100,
        color: _expanded ? Colors.blue : Colors.red,
        child: Center(child: Text(_expanded ? '收起' : '展开')),
      ),
    );
  }
}
```

### Hero 动画（页面跳转共享元素）

```dart
// 列表页
Hero(
  tag: 'product_${product.id}',  // 唯一标识
  child: Image.network(product.imageUrl),
)

// 详情页（相同 tag）
Hero(
  tag: 'product_${product.id}',
  child: Image.network(product.imageUrl, width: double.infinity),
)
// 跳转时自动产生共享元素动画
```

---

## 7. 常用第三方 UI 库

```yaml
dependencies:
  # 图片缓存（必装）
  cached_network_image: ^3.3.0

  # 图标库（更多图标）
  flutter_svg: ^2.0.0

  # 骨架屏
  shimmer: ^3.0.0

  # 轮播图
  carousel_slider: ^4.2.0

  # 下拉选择
  dropdown_button2: ^2.3.0

  # 日期选择器
  flutter_datetime_picker_plus: ^2.0.0

  # Toast
  fluttertoast: ^8.2.0

  # 权限管理
  permission_handler: ^11.0.0
```

### cached_network_image（图片缓存）

```dart
// 类比 UniApp 的 <image> 自动缓存
CachedNetworkImage(
  imageUrl: 'https://example.com/image.jpg',
  placeholder: (context, url) => Shimmer.fromColors(
    baseColor: Colors.grey[300]!,
    highlightColor: Colors.grey[100]!,
    child: Container(color: Colors.white),
  ),
  errorWidget: (context, url, error) => Icon(Icons.error),
  fit: BoxFit.cover,
)
```

### shimmer（骨架屏）

```dart
// 类比 UniApp 的骨架屏效果
Shimmer.fromColors(
  baseColor: Colors.grey[300]!,
  highlightColor: Colors.grey[100]!,
  child: Column(
    children: [
      Container(height: 200, color: Colors.white),
      SizedBox(height: 8),
      Container(height: 16, width: 200, color: Colors.white),
      SizedBox(height: 4),
      Container(height: 12, width: 150, color: Colors.white),
    ],
  ),
)
```

---

## 8. 手势处理

```dart
// GestureDetector（类比 @click @longpress @swipe）
GestureDetector(
  onTap: () => print('单击'),
  onDoubleTap: () => print('双击'),
  onLongPress: () => print('长按'),
  onHorizontalDragEnd: (details) {
    if (details.primaryVelocity! > 0) {
      print('向右滑动');
    } else {
      print('向左滑动');
    }
  },
  child: Container(
    width: 100,
    height: 100,
    color: Colors.blue,
    child: Center(child: Text('点我')),
  ),
)

// InkWell（带水波纹效果，推荐用于可点击元素）
InkWell(
  onTap: () => print('点击'),
  borderRadius: BorderRadius.circular(8),
  child: Padding(
    padding: EdgeInsets.all(16),
    child: Text('点击有水波纹'),
  ),
)
```

---

**上一章：** [07 - 网络请求与数据处理](./07-网络请求与数据处理.md)
**下一章：** [09 - 本地存储与原生能力](./09-本地存储与原生能力.md)
